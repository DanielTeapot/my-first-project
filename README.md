# my-first-project
his is my first project on GitHub.

## Pseudo code
﻿START PROGRAM

Create a list that can store 7 student grades

Ask the user to enter 7 grades

FOR each of the 7 grades
    Ask the user to enter a grade

    WHILE the grade is not between 1 and 100
        Tell the user that the grade must be between 1 and 100
        Ask for the grade again
    END WHILE

    Save the grade in the list
    
END FOR LOOP

Display all grades entered

Display the menu

    0. Exit
    1. Show highest grade
    2. Show lowest grade
    3. Show average grade
    4. Show most common grade
    5. Sort grades from lowest to highest
    6. Display all grades

 Ask the user to choose an option

    IF the user chooses 1
        Find and display the highest grade

    ELSE IF the user chooses 2
        Find and display the lowest grade

    ELSE IF the user chooses 3
        Calculate the average of all grades
        Display the average

    ELSE IF the user chooses 4
        Find the grade that appears most often
        Display it

    ELSE IF the user chooses 5
        Arrange the grades from lowest to highest
        Display the sorted grades

    ELSE IF the user chooses 6
        Display all grades

    ELSE IF the user chooses 0
        End the program

    ELSE
        Display "Not a valid option"

END PROGRAM
